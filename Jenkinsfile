pipeline {
    agent any

    stages {
    	stage('Aula Jenkins 1') {
				steps {
					sh '''

						docker compose version
						java --version
					    docker info
						
					'''
				}
    	}
    }
}
